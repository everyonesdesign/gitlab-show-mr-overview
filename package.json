{
  "name": "gitlab-show-mr-overview",
  "version": "0.0.2",
  "description": "This minimalistic extension makes Gitlab always show merge request overview",
  "license": "MIT",
  "private": true,
  "scripts": {
    "pack": "rm app.zip; cd app && zip -r ../app.zip *"
  }
}
